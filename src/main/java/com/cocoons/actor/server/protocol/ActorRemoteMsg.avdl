@namespace("com.cocoons.actor.server.protocol")
protocol ActorRemoteMsg {
    record ActorRemoteMsg {
    	int type; // 请求类型, 0, 请求; 1, 响应;
    	string sid;
    	string sender; // 发送者
        string receiver; // 接收者
        string name; // 消息实体类名称
        bytes msg;  // 消息实体
    }
}